# 阶段 0 进度日志

> **体裁：开发日志（过去时）。** 只追加，不删改既有段落；**不得作为实现依据**。
> 当前判定见[报告 0012](../spikes/SPK-0012-exit-criteria.md)；可复现的验证证据见 [`docs/spikes/`](../spikes/README.md)。

## 2026-09-16/17：原生 UI 与出口条件（自 `docs/ROADMAP.md` 移入，现见 `docs/plan/ROADMAP.md`）

> 原生编辑逐项验收见[报告 0021](../spikes/SPK-0021-native-edit-acceptance.md)：真实 IME、双源码、数学编辑、拖选/滚动与 Unicode 桌面场景通过；完整共同验收仍 Fail，槽位选区、大源码和未接入的窗口流程继续阻断。
> Typst 进程隔离和运行时白名单的后续进展见[报告 0018](../spikes/SPK-0018-typst-worker-isolation.md)，条件 6 仍部分满足。
> 后续入口隔离进展见[报告 0017](../spikes/SPK-0017-mixed-build-isolation.md)：普通混合构建 LaTeX 已接入统一沙箱，条件 6 仍部分满足。
> 以上只记时点状态。本文件定义**交付与出口条件**；**当前逐条判定以[报告 0012](../spikes/SPK-0012-exit-criteria.md) 为准**（阶段状态的唯一来源）。

## 2026-09-16：原生 UI 验证计划的状态段（自 `docs/NATIVE_UI_VALIDATION.md` 移入，现见 `docs/plan/NATIVE_UI_VALIDATION.md`）

> **当前状态（2026-09-16，UI 框架已选定，阶段 0 未关闭）**：候选比较已完成，最终选定 **egui / eframe 0.36.2**，
> 依据见 [ADR 0006](../adr/ADR-0006-native-ui-framework.md) 与报告 0001–0004；本页保留为验证方法与判据的记录。
> 后续新增选区、源码应用和预览见[报告 0016](../spikes/SPK-0016-working-tree-status.md)；
> 最新逐项桌面复核见[报告 0021](../spikes/SPK-0021-native-edit-acceptance.md)：六组限定桌面流程通过，完整共同清单仍 Fail；槽位选区、大源码、团队/恢复/完整预览集成和无障碍边界未闭合。
> 本页是**验证方法与判据**，其中的状态叙述只是副本；阶段 0 的**当前逐条判定以[报告 0012](../spikes/SPK-0012-exit-criteria.md) 为准**。

2026-09-16：用户确认技术栈与验证顺序。**候选实测已完成、框架已选定（egui），见 ADR 0006**；
本节的顺序与判据保留为历史记录。

## 2026-09-16：混合源码与团队编辑的阶段 0 验证状态（自 `docs/MIXED_SOURCE_EDITING.md` §10 移入）

> 本节只记时点结论与边界，属副本；阶段 0 的**当前逐条判定以[报告 0012](../spikes/SPK-0012-exit-criteria.md) 为准**。

上面的验收清单由阶段 0 的两项否决性验证部分关闭（**不是全部**）：

| 上面的条目 | 阶段 0 验证 | 结论与边界 |
|---|---|---|
| 1、2、3（同语言编辑、切换屏障、epoch / 离线 / 恶意写集） | [报告 0009](../spikes/SPK-0009-team-language.md) | **Pass（内存模型 + 确定性时序）**：60 用例 60/60，屏障扫描出跨方言许可窗口重叠 0、无屏障语言变化 0；旧 epoch 包被拒且不自动回灌。**真实网络、多进程、磁盘 fsync 与解析器未验证**；写集校验是字符串抽检，**不足以宣称能阻止恶意客户端**。 |
| 4、5、6、7（混合正文/跨页表格/宏/模板/双向引用、干净环境与失败夹具） | [报告 0010](../spikes/SPK-0010-mixed-build.md) | 36/36 宿主运行通过，含六类内容的成功/失败夹具；包重建的后续验证见[报告 0016](../spikes/SPK-0016-working-tree-status.md)。**明确不在范围**：复杂浮动体、超长文档、增量构建、目录/文献/脚注的跨引擎装配。 |

**仍未实现的部分**（与验证结论无关，属阶段 1 工作）：协调协议与桥接的正式实现
（[ADR 0001](../adr/ADR-0001-mixed-source-team-editing.md) 仍为 Proposed）；LaTeX/Typst 解析器与
reconcile 的正式实现（[ADR 0008](../adr/ADR-0008-reconcile-and-toolchain-isolation.md)）；
TeX 编译沙箱的产品集成与跨平台策略（Linux spike 已补良性编译及隔离验证，见[报告 0016](../spikes/SPK-0016-working-tree-status.md)；仍有只读运行时白名单等边界）。

## 2026-09-17：保存当前进展并复测检查点 3a85aae

按用户要求先将已有工作区提交为 `3a85aae`，再进行复测。该检查点包含报告 0022 的槽位选区工作，
以及后续的连续撤销、数学槽位源码投影、结构选区反馈、预览页面/帧指标与 PDF 检查沙箱改动；
本次未逐项审查这些实现，不能将报告 0022 的旧验证范围直接套用于整个检查点。

本轮使用 `CARGO_HOME="$PWD/spikes/native-ui/.cargo-home" cargo test --offline --manifest-path <manifest>`
分别复测四个 crate：

| manifest 所在目录 | 本轮结果 |
|---|---|
| `spikes/native-ui/core` | 45 通过 |
| `spikes/native-ui/candidate-egui` | 30 通过，2 ignored |
| `spikes/source-reconcile` | 4 通过 |
| `spikes/mixed-build` | 修正过时测试后 11 通过 |

混合构建首次复测有 1 项失败：旧 CLI 测试假定清空宿主 `PATH` 会禁用 PDF 检查工具，
但这些工具已在沙箱内使用固定路径。更新测试后，确认空 `PATH` 下 `E1-equation` 成功，
并通过将子进程 `TMPDIR` 指向 `/dev/null` 触发真实沙箱初始化失败，确认输出 `Fail` 且退出码非零。
两次构建在同一测试中顺序执行，避免共用夹具产物目录产生竞争。修改的测试通过 rustfmt，差异通过 `git diff --check`。

本轮未运行 ignored 大源码基准和预览集成测试、真实窗口/IME/AT-SPI 验收、全套混合构建夹具或完整 Clippy。
下一步仍需针对检查点新增路径完成真实窗口选区反馈、预览定位与呈现帧耗时验收；阶段 0 出口判定未升级。

## 2026-09-17：真实窗口续测、左侧排版修正与性能口径调整

完成整节点选区/撤销、AT-SPI、20 页翻页/缩放/段落定位等桌面续测，报告与原始证据见
[0023](../spikes/SPK-0023-native-layout-preview.md)。左侧为 core + egui 自绘，右侧才是 Typst 预览；
窗口 CPU 计时覆盖三个面板，不能说成左侧单独渲染或 Typst 编译时间。

用户指出左侧错位并允许重写，同时明确撤销没有依据的固定 15/16 ms 门槛。
本轮先修正数学槽位仅画首个子节点、根号基线、负坐标归一化高度及 egui 字体实际基线偏移，
补数学间距和分数留白。没有重写为完整排版引擎，真实字宽、完整选区几何和可伸缩数学符号仍待做。

core 47 项、egui 30 项通过（2 ignored），egui Clippy 通过。更新后 release CPU 组内 p95 为
7.608–9.126 ms，Typst 编译加栅格化 2154 ms；只报告数据，不按固定帧预算宣布 Pass/Fail。
桌面首轮 IME 提交超时与选区场景焦点变化均保留证据，随后两项定向复测通过。
下一步优先统一字体测量、绘制和光标几何，再处理完整结构外框；阶段 0 状态不升级。

## 2026-09-17：左侧文字与交互几何统一

接续报告 0023，将 egui galley 的实测几何接入文字绘制、光标/IME 锚点、点击命中、
文本选区和 AT-SPI 字符框。按 revision/屏幕缩放缓存，并按 NodeId 检索；点击仍只落在字素边界。
本轮未重写结构布局，其文字尺寸估算、完整结构外框及多行选区仍待处理。

egui 32 项测试通过、2 ignored，Clippy 和格式通过。七组真实编辑场景通过；
多页预览首次因 `/tmp` Typst 工具丢失失败，指定已有 0.15.1 工具后复测通过，保留两次证据。
窗口 CPU 组内 p95 为 7.546–8.523 ms，编译加栅格化 1644 ms，不按固定毫秒门槛判定。
报告与截图见 [0024](../spikes/SPK-0024-text-geometry.md)。下一步让结构布局使用实测文字尺寸，
再完善根号/分数等结构外框；阶段 0 状态不升级。

## 2026-09-18：数学结构实测尺寸与完整节点盒

接续 `d0cde89`，为 core 布局增加可选字体测量接口，egui 用同一 galley 的宽高和基线参与数学结构排列。
节点盒随结构合成和平移保留，整节点选框包含根号、横线及后代；空数学槽位保留可编辑宽度与光标。
矩阵布局拆为独立文件以保持文件规模限制。未新增依赖，也未把左侧编辑器改为 Typst 编译。

core 47 项、egui 34 项通过（2 ignored）；egui Clippy、格式和差异检查通过。
八组真实桌面场景全部通过，选区截图已确认覆盖根号及上横线。窗口 CPU 组内 p95 为
8.901–10.543 ms，编译加栅格化 4028 ms，继续仅记录实测、不按固定毫秒数验收。
报告及原始证据见 [0025](../spikes/SPK-0025-measured-structure-layout.md)。
后续优先验证可伸缩根号/括号与更深嵌套结构；多行选区、数学字体常量及共同验收其它缺口仍保留。

## 2026-09-18：确认自绘拼接不可作为最终视觉渲染

对比同一夹具的左侧截图与 Typst 页面后确认，分数、根号、上下标、字形和间距差异来自两套排版系统，
继续调近似常数无法达到可接受视觉质量。实现改为：当前 revision 有 Typst 结果时，左侧正文 painter 直接绘制
同一页 Typst 图像；编译中/过期/失败时回退结构视图。编辑、命中、光标、选区、IME、AT-SPI 仍由原生模型层处理。

第一次把图片作为普通控件放在正文滚动区外导致左侧空白，已改为画布 painter 绘制并保留失败证据；
定向真实窗口复测整节点选区和多页预览通过。报告与截图见 [0026](../spikes/SPK-0026-typst-visual-layer.md)。
下一步不是继续美化近似拼接，而是补 Typst source map 到左侧图像坐标的精确光标/选区覆盖；阶段 0 状态不升级。

## 2026-09-18：否决整页 Typst 覆盖方案

对比同一夹具的左侧截图与 Typst 页面后确认，分数、根号、上下标、字形和间距差异来自两套排版系统，
继续调近似常数无法达到可接受视觉质量。实验把当前 revision 的 Typst 页面绘制到左侧后，视觉质量确实改善，
但页面缩放坐标与原生模型的命中、光标和选区脱钩；该实现已撤回。报告 0026 的对比截图和脚本保留，
下一步必须补 Typst source map 到节点级几何，再决定如何叠加原生交互；阶段 0 状态不升级。

## 2026-09-18：结构化 Typst scene 隔离验证

新增 scene 探针，保留 Typst 绘制组并递归提取 page-space 字形几何。
行内/块级分数、上下标、二维矩阵、嵌套根式、旋转公式的重绘与 Typst 原图为零像素差异；
14 个公式字母命中源码 span，egui 离屏纹理及三档缩放/滚动坐标测试通过。
实现和边界见[报告 0027](../spikes/SPK-0027-structured-typst-frame-research.md)。
尚未接入真实编辑窗口、SDG 光标和空槽位，不宣称左侧视觉问题已经修复，阶段 0 状态不升级。

## 2026-09-18：左侧切换为 Typst 编辑画面

按用户要求替换左侧，新增隔离编译 helper 返回同 revision 图像与 glyph/shape/source-span 几何，
用于节点定位、光标、选区、IME、空槽位与 AT-SPI；编译中保留旧画面，停止旧坐标定位。
左右显示共用编辑投影。真实窗口七组场景通过，失败与复测证据见[报告 0028](../spikes/SPK-0028-typst-editor.md)，
架构边界见[ADR 0013](../adr/ADR-0013-typst-editor-scene.md)。旧布局只留作显式回归测试探针。
每次请求仍启动新编译进程，响应优化、完整字符映射和发布源码投影差异待处理；阶段 0 不升级。

## 2026-09-18：编辑画面背景与空槽位修复

按用户截图反馈，将左右显示改为透明背景、UI 主题文字色着色；空槽位改用编译器定位的无墨迹 box
和 metadata，去除末尾方框但保留编辑定位。三项真实沙箱映射测试与 Clippy 通过，两个定向桌面场景
通过并人工核对截图，原空叶子可输入 END。证据见[报告 0028](../spikes/SPK-0028-typst-editor.md)。
本轮未解决每次冷启动编译带来的输入延迟，也未宣称浅色主题桌面验收；阶段 0 不升级。

## 2026-09-18：缩小编辑编译运行时清单

确认逐请求准备完整 TeX 工具环境会增加数百毫秒启动耗时。编辑 helper 接入最小运行时清单，
保留 OS 隔离和全部资源限制，默认其它工具链清单不变。同一单页夹具五次对比的编译导出中位数
由 1044.10 ms 降至 546.44 ms，PNG 和几何哈希完全一致；这不是端到端键入延迟，也不含 UI 解码。
隔离/超时探针、三项沙箱映射、35 项常规回归、Clippy 通过；三个桌面场景通过并复核截图。
原始数据与复现脚本见[报告 0028](../spikes/SPK-0028-typst-editor.md)。仍需处理冷启动和增量缓存，阶段 0 不升级。

## 2026-09-18：持久 Typst 编辑会话与局部纹理上传

用户继续反馈慢和整页刷新，确认逐请求冷启动不适合作为即时编辑路径。后台改为受监督沙箱会话，
复用 World、字体和 Source；按完整场景清单复用图像块，UI 保留纹理并局部上传，状态栏固定单行。
标准夹具连续输入时每次更新 4–5/70 块，后台处理中位数 17.924 ms；首次仍约 358 ms，
这不是输入到 GPU 呈现延迟。首次 IME 因窗口失焦中止，复测通过；其它八项窗口场景通过并核对截图。
像素/映射独立对比、跳过中间结果、多页缩减、超时与父进程退出检查通过。
helper 严格 Clippy 尚有旧探针诊断，未声称全仓检查通过。
规范见[ADR 0014](../adr/ADR-0014-resident-typst-editor.md)，证据见[报告 0029](../spikes/SPK-0029-resident-typst-editor.md)。
helper 仍逐页栅格化；大文档、端到端输入和长期会话验证继续保留，阶段 0 不升级。

## 2026-09-18：按用户裁决收口编辑性能验证

用户确认当前延迟已无明显体感，要求停止精细优化、推进功能。本轮常用窗口编辑、多页预览、
连续输入全部通过，新增 256 段/26 页首段输入、删除、撤销与末页输入测试。
首次大文档测试暴露同帧 Ctrl+Z 按下/松开漏识别，新增失败回归后修为读取键事件修饰键，复测通过。
全阶段脚本首轮 33 过、2 失败，定位为导出包清理删掉所引用的 Typst CLI；脚本改为启动时保留工具副本，
失败与复测证据分别保存。报告见[0030](../spikes/SPK-0030-editing-regression.md)。
条件 3 按已测夹具收口，GPU 精细计时和局部栅格化不再阻塞推进；阶段仍有原生完整集成、安全入口审查和
混用保真三项部分满足。下一步优先窗口打开/保存/重开和草稿恢复，不搭生产脚手架。

本轮收尾：两项工具路径相关失败的定向复测均通过，导出包 28/28 重建成功；
实际检查合并覆盖 35 项，保留首轮失败和复测原始日志，不声称首次全量运行无失败。

## 2026-09-19：候选窗口实验会话恢复

确认属于原 UI 计划文件恢复项后，接入显式开启的实验会话文件：后台打开/保存/重建、
自动 checkpoint、窗口重开、正文与独立源码草稿恢复。错误与外部冲突保留内存内容和磁盘字节，
恢复重建渲染会话，本地撤销不包含导入动作。边界见 [ADR 0015](../adr/ADR-0015-ui-session-recovery-spike.md)。
默认候选测试 42 过、11 ignored，Clippy 与许可/来源通过；真实窗口 LaTeX 强杀恢复、
Typst 过期草稿重启拒绝提交通过。首轮失焦/AT-SPI 取证失败及复测均保留，见
[报告 0031](../spikes/SPK-0031-ui-session-recovery.md)。不扩展正式项目格式、共享身份或历史持久化。
正常关闭已测持久化后的退出；关闭中故障和断电边界未宣称通过。阶段状态不升级。

## 2026-09-19：团队语言门禁窗口模拟

按用户要求先推送 `adc6bd9`，再推进窗口集成。复用原严格语言协调器，单窗口模拟三成员，
独立草稿、许可/epoch、显式屏障确认和离线拒绝；协调检查与 reconcile 全通过才发布正文。
原协调器 60/60、候选默认测试 45 过/11 ignored、两 crate Clippy 与候选许可通过。
真实窗口三成员顺序提交、缺失确认、离线、旧 epoch 与异语言拒绝、新语言成功全部通过并核对截图。
见 [报告 0032](../spikes/SPK-0032-team-window-gate.md) 与 [ADR 0016](../adr/ADR-0016-team-window-gate-spike.md)。
团队模式正文只读、草稿仅在进程保留；不宣称真实网络、CRDT 合并、协作撤销或 IME 屏障已通过。
阶段状态不升级，后续仍按出口清单补齐，不搭生产框架。

## 2026-09-19：构建入口审查与 PDF 清单收紧

以 `70fd27d` 审查当前编译/PDF 入口，发现 PDF 探针虽已隔离仍共用 full 清单，改为专用
pdf-probe 工具与资源白名单，新增正常 PDF 四工具对照和挂载正/负对照，加入安全常规门禁。
安全 5/5、混合构建 36/36、验收脚本自测通过。初次 Clippy 受全 crate 格式化导致旧函数行数
变化影响，撤回无关格式化后通过；失败日志保留。入口/运行时矩阵见
[报告 0033](../spikes/SPK-0033-build-entry-audit.md)，决策见 [ADR 0017](../adr/ADR-0017-pdf-probe-runtime.md)。
旧 recovery 仅暂存目录、不具 OS 沙箱，研究 helper 手动运行也不自动隔离；都不宣传为安全项目入口。
条件 6 保持部分满足，资源树例外、总配额和解析器安全边界继续保留。

## 2026-09-19：源码 include 行内与内部引用验证

以 `427c73c` 新增双宿主真实 rebuild 夹具，先复现同语言 include 错用矢量组件锚点导致的链接缺失，
再修为原生标签引用并从宿主收集 include 符号。行内 include 新增内容类型限制，跨语言行内矢量仍拒绝。
最终 PDF 验证宿主第 1 页与组件第 2 页双向链接，及两侧文字/行内 x 同行位置；人工核对双宿主截图。
混合构建 36/36、双宿主集成、2 条门禁、Clippy 与脚本自测通过。失败复现和取证修正日志保留，
见[报告 0034](../spikes/SPK-0034-native-include-fidelity.md)。不宣称跨语言矢量基线/内部注释或精确目的地坐标完成。

## 2026-09-19：跨语言行内语义投影

将受支持的跨语言 Inline + Vector 从直接拒绝改为受限语义投影：单行文本、行内公式与引用直接由宿主
引擎生成，进入宿主行盒；块级内容与宏桥接仍在计划层拒绝。外语组件继续独立编译验证源语义，但最终
行内公式基线与内部链接由宿主布局产生。LaTeX 宿主/Typst 组件和 Typst 宿主/LaTeX 组件各自真实
`--rebuild`，最终 PDF 检查同行位置和宿主页内链接目标均通过。阶段 0 仍保持部分满足：复杂跨语言块级
载体内部注释、同页精确目的地坐标和无障碍语义尚未覆盖。
