# 2026-09-24：公式块输入失败修复

用户要求先提交页面直接编辑，已形成带 DCO 的 `ab187ff`。
随后用户澄清问题发生在公式块内，同时感觉编译延迟大。

复现了 `alpha` 输入到 `al` 的未知变量错误：旧页面不更新，造成文字未显示的体验。
改为在当前页面原位置显示未完成公式文字及真实诊断，补全后恢复正常排版；没有另加输入框。
并提前初始化字体、采用请求/结果主动唤醒、复用单个 TextItem 几何缓冲，修复返回可视模式后丢焦点。

真实独立公式按钮路径复测与测量见[证据](../spikes/evidence/SPK-0005/formula-input/README.md)。
暖态短公式提交至栅格返回约 11–12 ms，不包含去抖、显示刷新或冷字体扫描，不能泛化为长文档性能。

最终工作区 67 项测试通过，构建、clippy、格式、文档 warnings 与文件规模检查通过。
本次修复保留为 `ab187ff` 之后的独立工作区改动。
