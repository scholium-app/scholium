[package]
name = "scholium-spike-untrusted"
version = "0.0.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"

[workspace]

[dependencies]
scholium-spike-core = { path = "../native-ui/core" }
typst = "=0.15.1"
typst-layout = "=0.15.1"
typst-kit = { version = "=0.15.1", features = ["embedded-fonts", "scan-fonts"] }

[lints.rust]
missing_docs = "warn"
